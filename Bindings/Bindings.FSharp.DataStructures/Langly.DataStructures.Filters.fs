namespace Langly.DataStructures.Filters

/// <summary>
/// Represents a filter, a psuedostructure which helps a proper data structure to behave differently.
/// </summary>
type filter< ^index, ^element> = Filter< ^index, ^element>
